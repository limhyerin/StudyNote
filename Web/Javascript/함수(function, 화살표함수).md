# 🌻함수(Function)🌻

<br/>

## 🌼기본 함수(function)🌼
```js
function sum(a,b) {
  return a + b;
}
```

<br/>

## 🌼화살표 함수🌼
```js
const sum = (a,b) => {
  return a + b;
}
```
